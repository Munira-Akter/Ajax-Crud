<?php

// database connection

$host = 'localhost';
$root = 'root';
$pass = '';
$db = 'auth';


$way = new mysqli ($host, $root, $pass, $db);

    // $host = 'localhost';
    // $user = 'root';
    // $pass = '';
    // $db = 'facebook';

    // $connection = new mysqli($host, $user, $pass, $db);